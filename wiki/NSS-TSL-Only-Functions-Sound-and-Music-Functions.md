# Sound and Music Functions (TSL index)

Part of the [NSS File Format Documentation](NSS-File-Format).

**Category:** TSL-Only Functions

**Shared** music and sound NWScript routines (same names in both games; **routine IDs** differ by build) are documented in **[NSS-Shared-Functions-Sound-and-Music](NSS-Shared-Functions-Sound-and-Music)**.

The master [NSS-File-Format](NSS-File-Format) table of contents also lists **TSL-only** symbols under **TSL-Only Functions → Sound and Music Functions** (`DisplayDatapad`, `DisplayMessageBox`, `PlayOverlayAnimation`). Those entries are indexed there; when expanded, they will follow the same hub pattern as other TSL categories.

This page name is kept so links from [NSS-File-Format](NSS-File-Format) keep resolving.
